/**
 * Dashboard route ("/") — placeholder for Task 1.
 *
 * The real instrument-panel dashboard (vehicle rows, odometer readouts,
 * annunciator lamps, interval gauges — see .superpowers/sdd/globals.md)
 * is built in Task 8. This page only proves the scaffold, tokens, fonts,
 * and nav shell all wire together correctly.
 */
export default function Home() {
  return (
    <div>
      <h1 className="eyebrow">Dashboard</h1>
      <p className="mt-3 text-steel">Instrument panel lands in Task 8.</p>
    </div>
  );
}
